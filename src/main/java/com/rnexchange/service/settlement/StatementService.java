package com.rnexchange.service.settlement;

import java.util.UUID;

/**
 * Service for assembling per-account daily statements and related views.
 */
public interface StatementService {
    /**
     * Placeholder to signal initial skeleton; concrete methods will be introduced in later phases.
     */
    default void placeholder(UUID statementId) {
        // no-op
    }
}
